---
tipo: devlog
data: 2026-04-25
agente: Claude Opus 4.7
sessione: fase2-fix-bugs-antigravity
commit: 7cc6866 (branch staging)
tags: [security, schema, stripe, gc, devlog]
---

# Devlog 2026-04-25 — Opus, Fase 2 Fix Bugs

## Contesto

Salandra ha approvato in chat il piano [[Plan-Fix-Bugs-Antigravity]] (e implicitamente la prosecuzione di [[Plan-Stripe-Recovery]] §2). Esecuzione delegata a me come Executor da [[Protocollo-Collaborazione]] §1.

Lettura preliminare del vault eseguita: protocollo, memoria, plan attivo, post-review precedente, backlog, [[Stato-Progetto-2026-04-25]].

## Cosa ho fatto

### Fix di sicurezza (sprint 1)
- **`.gitignore`** esteso: aggiunti `CREDENZIALI.md`, `*.credentials`, `supabase/.temp/`, `vault/.obsidian/`, `vault/Senza nome*.canvas`. Risolve BUG-007 (profilattico, il vault non era ancora committato → nessun leak su GitHub) e BUG-011.
- **`middleware.js`**: matcher esteso da `['/admin/:path*']` a `['/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)']`. Ora il redirect HTTP→HTTPS gira su tutto il sito, non solo su `/admin`. Risolve BUG-008.

### Cleanup codice (sprint 2)
- **`lib/rate-limit.js`**: spostato `import { NextResponse } from 'next/server'` da riga 70 a inizio file. Risolve BUG-009.

### Schema riallineamento (sprint 3 — BUG-002)
Procedura:
1. Estratto via Supabase MCP (`execute_sql` su prod `ddqwutxocznggfmrzzkw`):
   - `information_schema.columns` → tutte le colonne reali (incluse `stalls.latitude/longitude`, `events.image_url/map_lat/lng/zoom` che mancavano nello schema di Antigravity)
   - `pg_get_viewdef('stalls_with_status')` → la view reale usa LATERAL join con stati `blocked/booked/pending/free` e include latitude/longitude
   - `pg_get_functiondef` per is_admin, stall_status_of, stall_vendor_name, audit_trigger, enforce_booking_limit, vendors_touch, delete_my_account, anonymize_old_bookings, purge_old_audit_log, generate_stalls, release_expired_pending_bookings
   - `pg_policies` → tutte le RLS policy attuali
2. Riscritto `supabase/schema.sql` (520 righe) come dump fedele e idempotente.
3. Spostate le 8 migrazioni storiche in `supabase/migrations-archive/` con prefisso numerico (02-09) e README esplicativo.
4. Create le migration "vive" in `supabase/migrations/`:
   - `13_stripe_events_seen.sql`
   - `14_stripe_gc_pending_bookings.sql`

### GC Stripe pending (sprint 4 — BUG-006 completion)
- Migration `14_stripe_gc_pending_bookings.sql` applicata via Supabase MCP su **prod e staging**.
- `pg_cron` extension installato (schema `extensions`).
- Funzione `release_expired_pending_bookings()` SECURITY DEFINER + cron job ogni 5 min.
- Cancella bookings `pending` più vecchi di 15 min (li mette a `cancelled`, mantiene audit log).

### Vault committato
- Tutto `vault/*.md` ora è in repo. La sotto-cartella `.obsidian/` resta locale (gitignored).
- Il vault diventa ufficialmente il "cervello condiviso" portabile fra Opus, Antigravity, Codex.

## Cosa NON ho fatto (richiede input umano)

| Bug | Cosa serve | Da chi |
|---|---|---|
| BUG-004 | `STRIPE_SECRET_KEY` (test+live) e `STRIPE_WEBHOOK_SECRET` da configurare su Vercel | Salandra deve fornirle in chat |
| BUG-004 | `SUPABASE_SERVICE_ROLE_KEY` da configurare su Vercel (Production+Preview, distinte) | Salandra deve copiarla da Supabase Dashboard → Settings → API → service_role |
| BUG-010 | `SITE_URL` di Supabase Auth staging punta a localhost | Salandra in Supabase Dashboard staging → Auth → URL Configuration → Site URL = `https://soresina-mercati-git-staging-...vercel.app` |
| BUG-012 | `SENTRY_AUTH_TOKEN` su Vercel per upload source maps | Salandra crea token su sentry.io (scope `project:releases` + `org:read`) e me lo passa |

Quando Salandra fornisce le keys posso configurare tutto via Vercel API in 2 minuti.

## Verifiche eseguite

- `node --check` su tutti i file JS modificati: ✅ sintassi OK
- Migrations 13 e 14 applicate con `success: true` su prod (`ddqwutxocznggfmrzzkw`) e staging (`yctfshlwgouhppadptgy`)
- `git status` pulito post-commit (a parte i file `.obsidian/` ora correttamente ignorati)
- Push `origin staging` ok: `12521ed..7cc6866 staging -> staging`

## Stato del repository

- HEAD `main`: `5989389` (Sentry activate) — invariato. Prod produzione live e funzionante (vecchio flusso, no Stripe).
- HEAD `staging`: `7cc6866` (questo commit). Vercel deployer triggerato.
- Working tree: pulito.

## Cosa rimane aperto

Bug nel backlog ancora `aperto`:
- BUG-004: env Stripe Vercel
- BUG-010: SITE_URL Supabase staging
- BUG-012: SENTRY_AUTH_TOKEN

Tech debt non bloccanti: 5 voci, nessuna critica.

Quando Salandra fornisce le 4 secret keys (Stripe ×2 + Supabase service role + Sentry token) procedo con la configurazione finale e lo smoke test end-to-end di Stripe su staging. Dopo lo smoke test passa, possiamo mergiare `staging → main` per portare Stripe in prod.

## Per Antigravity

Quando rileggi il vault: i tuoi BUG-007/008/009/006/002/011 sono tutti chiusi in commit `7cc6866`. Lo `schema.sql` consolidato che hai proposto è stato rifatto da zero (dump dal DB reale) come da §3.1 del tuo piano. Le migrazioni storiche sono archiviate, non cancellate. Plan eseguito al 100% per la parte autonoma; le 4 voci con secret keys aspettano Salandra.
